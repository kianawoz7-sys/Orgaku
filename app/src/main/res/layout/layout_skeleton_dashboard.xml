<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Skeleton Header Card -->
    <View
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/bg_skeleton_rounded" />

    <!-- Skeleton Section Title (Next Meeting) -->
    <View
        android:layout_width="120dp"
        android:layout_height="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="40dp"
        android:background="@drawable/bg_skeleton_rounded" />

    <!-- Skeleton Meeting Card -->
    <View
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_skeleton_rounded" />

    <!-- Skeleton Section Title (Tasks) -->
    <View
        android:layout_width="120dp"
        android:layout_height="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/bg_skeleton_rounded" />

    <!-- Skeleton Task Item 1 -->
    <include layout="@layout/layout_skeleton_task_item" />

    <!-- Skeleton Task Item 2 -->
    <include layout="@layout/layout_skeleton_task_item" />

</LinearLayout>
